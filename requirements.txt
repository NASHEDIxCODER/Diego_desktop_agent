# Diego Desktop Agent runtime dependencies
#
# Derived from project imports. This is a top-level requirements file, not a
# frozen dump of every transitive dependency from one local environment.
#
# SUPPORTED PYTHON: production is pinned to Python 3.11 via
# requirements.runtime-lock.txt (the fully-resolvable, fully-tested
# combination). Python 3.14 boots via compat.py stdlib shims, but the
# optional heavy stacks (Coqui TTS <3.12, paddlepaddle <3.13, kokoro/
# misaki 0.9.x) are NOT installable on 3.14 — 3.14 is a degraded/partial
# runtime, not a supported production target.

# Core configuration, services, and persistence
aiohttp>=3.9.0
duckdb>=1.0.0
httpx>=0.27.0
numpy>=1.26.0
packaging>=24.0
psutil>=5.9.0
pydantic-settings>=2.0.0

# Local LLM, embeddings, and NLP
google-generativeai>=0.8.0
huggingface-hub>=0.23.0
numba>=0.59.0
rapidfuzz>=3.0.0
sentence-transformers>=2.4.0
sentencepiece>=0.2.0
spacy>=3.7.0
torch>=2.1.0
tqdm>=4.66.0
transformers>=4.40.0

# Voice input, wake word, and ASR
audioop-lts>=0.2.0; python_version >= "3.13"
faster-whisper>=1.0.0
jellyfish>=1.0.0
onnxruntime>=1.17.0
openwakeword>=0.4.0
scikit-learn>=1.4.0
scipy>=1.11.0
sherpa-onnx>=1.10.0
silero-vad>=5.1
sounddevice>=0.4.6
SpeechRecognition>=3.10.0

# Optional ASR runtimes used by voice/providers/nemotron_provider.py
llama-cpp-python>=0.2.0
nemo-toolkit[asr]>=2.0.0

# Streaming TTS providers and fallbacks
# Known-good pair (matches requirements.runtime-lock.txt). kokoro 0.7.x
# requires misaki>=0.7.16, which has no distribution for Python 3.14 —
# the 0.9.x pair is the resolvable combination on the supported 3.11
# runtime. On 3.14 these report "no matching distribution" honestly
# (TTS falls back to pyttsx3; see docs/DOCKER_RUNTIME_MATRIX.md).
kokoro>=0.9.4
misaki[en]>=0.9.4
piper-tts>=1.6.0
pyttsx3>=2.90
TTS>=0.22.0; python_version < "3.12"

# Face authentication and computer vision
face-recognition>=1.3.0
firebase-admin>=6.0.0
mss>=9.0.0
opencv-python>=4.8.0
Pillow>=10.0.0

# Knowledge document extraction (pdf/docx/pptx/xlsx)
PyPDF2>=3.0.1
python-docx>=1.1.0
python-pptx>=1.0.0
openpyxl>=3.1.0

# OCR, screen understanding, and accessibility
beautifulsoup4>=4.12.0
easyocr>=1.7.0
lxml>=5.0.0
paddleocr>=2.8.0
paddlepaddle>=2.6.0; python_version < "3.13"
PyGObject>=3.46.0
pytesseract>=0.3.10
trafilatura>=1.12.0

# Desktop automation and browser control
playwright>=1.45.0
PyAutoGUI>=0.9.54
pyperclip>=1.8.2
selenium>=4.20.0

# Desktop UI (PySide6)
PySide6>=6.5.0

# Integrations
Telethon>=1.34.0

# Test suite
pytest>=8.0.0

# Post-install notes:
# - Run: python -m playwright install chromium
# - Install system packages as needed: tesseract-ocr, portaudio/pipewire audio
#   libraries, GTK/AT-SPI development libraries for PyGObject, and dlib build
#   dependencies for face-recognition.
