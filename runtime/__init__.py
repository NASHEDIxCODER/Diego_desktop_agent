"""
Runtime package — Leo's production runtime layer.

Contains the LeoRuntime orchestrator, the service layer, the state
machine, the supervisor, metrics, the dashboard, the tool registry,
and the background agent.
"""